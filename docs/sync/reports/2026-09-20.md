# Upstream sync report — 2026-09-20

Status: **GREEN**. Released v0.86.0 fixtures regenerate without differences and pass the complete
Linux race and pure-Go wire suites. The release pin is promoted after that successful candidate gate.

## Revisions

- Base: `107d79f11072bbc8a3a757ed7fd69596bee7d68c` (`v0.85.0`).
- Released target: `ecac0a9c4edad3dac5d9f8b40e0c7db7a56471fc` (`v0.86.0`).
- Release: https://github.com/earendil-works/pi/releases/tag/v0.86.0
- Delta: 177 commits and 594 changed files, including the intervening v0.85.1 release.
- Sync classification: 47 wire-format, 13 API-surface, 463 feature-only, and 71 documentation paths.

## Production compatibility

The owner requested source-compatible headless adoption. Existing exported Go function signatures,
interfaces, legacy `ai.Context` fields, and legacy session reads are retained. An exported-API
snapshot of the pre-change revision is used for comparison.

The owner explicitly approved Pi's default transcript system-message events after reviewing the
actual Ordalie consumer. This is an observable behavior change, even with unchanged Go signatures.
Before deploying the upgraded dependency, update Ordalie's `agentengine` message-event forwarding:
`interaction.go` currently forwards every `message_end`, and `worker_pool.go` publishes that payload
over SSE. Filter system messages from client-facing events; their content includes system prompts
and model-visible tool declarations. Internal persistence/replay consumers may retain them.

The backend also uses the shipped faux provider in multi-round headless tests. Those tests must
accept transcript system messages while retaining the legacy prompt/tool fields seen by existing
stream functions. Backend source and dependency files are not modified by this Orb change.

## Adoption scope

- Transcript-backed prompt sections and tool declarations, session checkpoints, and provider replay.
- Provider request/cache/reasoning corrections, error classification, and usage pricing fixes.
- RPC input interception, extension dispatch/unsubscribe/schema handling, compaction budgets and cancellation.
- Header-first session discovery and progressive, cancellable resume with stable selection.
- CJK autocomplete and clipboard completion/failure handling.
- Built-in strict-prefer tool schemas and correct shell termination reporting.

Prompt-cache warming and local diagnostics export are deferred. Radius uploads, native clipboard
helpers, and parallel experimental Pico/durable runtimes remain excluded from this adoption.
Model generation continues to follow D12's direct authoritative sources with released metadata rules.

The old product-name audit found no genuine references in tracked files; substring matches inside
`APIGoogle*` are provider identifiers and are retained.

## Verification

- Pre-change Linux binary: all 29 pinned upstream RPC tests passed across six test files.
- Integrated candidate Linux binary: the same 29 pinned v0.85 RPC tests passed across six test files.
- Released TS Pi source CLI and final Orb candidate: the same 29/29 unmodified v0.86 upstream RPC
  tests pass against the same local mock. Fresh sessions exclude their empty bootstrap system/tool
  message from `messageCount`, while `get_messages` retains it; explicit system messages count normally.
- Session discovery/picker: focused race tests, F6 listing comparison, and vet passed.
- Built-in tools: v0.86-extracted fixtures, Linux execution, race tests, pure-Go tests, and vet passed.
- Go 1.27.1 exported-API comparison against the baseline: no incompatible changes reported.
- Linux/arm64 auth cross-compatibility passed against released v0.86: TS reads Orb's `auth.json`,
  legacy migration stays byte-compatible, and reciprocal lock contention passes under the race detector.
- Baseline passed all three selected Ordalie headless/SSE tests. The first candidate run passed
  multi-round tools and persistence/SSE, but lost the legacy system prompt on a subsequent tool
  round. The repaired candidate now passes the same three tests in `ordalie-go-api-blue` with
  Go 1.27.1; backend source, dependency files, and running service remain unchanged.
- Legacy `engine.Agent.SetSystemPrompt` reaches the actual provider transcript on subsequent calls.
  Transcript-aware session preparation remains authoritative, and memory attachment uses the setter
  directly rather than carrying a separate transcript-rewrite workaround.
- Linux/arm64 session cross-read: released TS v0.86 reads and projects Orb's session JSONL;
  `ORB_F6_TS_VERIFY=1` passes under the race detector.
- Complete `make check` passed on Go 1.27.1/Node 24.18.0 before final full-fixture refresh.
- Final exported-API comparison, canonical Linux fixture regeneration, production-consumer replay,
  29/29 upstream RPC checks, and all four pure-Go cross-builds pass. Final post-pin `make check`
  passed: build, vet, lint (0 issues), full race suite, and pure-Go wire suite.
- Tier-2 live provider smokes: required credential environment variables are not set; not run.

The owner subsequently required the latest stable Go for every update. go.dev confirmed Go
1.27.1; `go.mod`, the contributor policy, and current documentation now follow it. CI and releases
already read `go.mod`. golangci-lint is upgraded to 2.13.2 and rebuilt on toolchain/pin changes.
The model catalog was regenerated with Go 1.27.1: its embedded gzip output changed with the
standard-library encoder. Repeated generation is byte-identical, and race/pure-Go catalog checks pass.
Fixture tooling now checks all 27 pinned dependencies against the released versions. This matters
for Azure: Pi's pinned OpenAI SDK 6.40.0 preserves the odd `custom=true%2Fresponses` query value,
where the stale 6.26.0 extraction removed it. Orb follows the released SDK; the golden is regenerated
from that SDK. Google SDK 2.21.0 also changes object-field order; Orb's Gemini/Vertex encoders
and regenerated request goldens now match it byte-for-byte. Unused Mistral-SDK and glob fixture
installs were removed.
Final validation targets Go 1.27.1 and Node 24.18.0. Node 26 intentionally no longer transforms
TypeScript enums, which the pinned F13 ecosystem package uses; use the supported runtime rather
than skipping tests or changing extension-runtime policy.

Pre-change darwin/arm64 release baseline (`-trimpath -s -w -funcalign=4`, Go 1.26.6):
36,293,714 bytes; 20 warm-cache `--version` launches average 8.52 ms (median 8.27 ms).

Integrated Go 1.27.1 release-style builds (`CGO_ENABLED=0`, same flags) pass on all four targets:
Linux amd64 37,019,808 B; Linux arm64 34,799,776 B; Darwin amd64 37,911,968 B; Darwin arm64
35,900,146 B. Darwin arm64 is 1.08% smaller than the baseline. To control for concurrent integration
load, the final startup comparison alternates baseline/candidate order for 20 warm-cache launches
each: baseline mean 11.17 ms / median 10.77 ms, candidate mean 11.14 ms / median 10.83 ms
(candidate range 9.01–16.65 ms). Startup is effectively unchanged and below the 50 ms budget.

The sync classifier now includes transcript utilities and structured prompt assembly as kernel
paths. Candidate conformance also runs the shipped `CGO_ENABLED=0` wire tests after the race suite;
race-only arm64 builds do not exercise the same floating-point contraction behavior.
The canonical Linux dry run regenerated every upstream fixture with zero byte differences.
It exposed a sync-copy bug: an upstream-emitted F12 command file could override Orb's owned snapshot.
The copy/promotion path now preserves Orb snapshots even when extraction emits the same family,
with a regression test. The embedded release changelog's presentation snapshot is refreshed through
`make fixtures-tui`; behavior-shaped values remain preserved.
Final sync conformance is green on Linux/arm64 with Go 1.27.1 and Node 24.18.0, including the full
race suite and pure-Go wire suite. Docker verification uses `--init` so orphaned shell children are
reaped, as on normal CI hosts; the process-group cleanup assertion stays enabled.
